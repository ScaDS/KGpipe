[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kgpipe-examples"
version = "0.1.0"
description = "Examples for KGpipe"
requires-python = ">=3.12"
dependencies = [
    "kgpipe",
]

[tool.setuptools.packages.find]
where = ["src"]

